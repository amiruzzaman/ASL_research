---
import type { HTMLAttributes } from "astro/types";

export type Props = HTMLAttributes<"canvas">;

const { class: className, ...props } = Astro.props;
---

<canvas class:list={[className, "bg-base-200 rounded"]} {...props}></canvas>
